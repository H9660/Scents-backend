{
  "version": 2,
  "builds": [
    {
      "src": "**/*.ts",
      "use": "@vercel/node",
      "config": { "includeFiles": ["dist/**"] }
    }
  ],
  "routes": [{ "src": "/api/(.*)", "dest": "dist/index.js" }]
}
